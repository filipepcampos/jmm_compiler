import MathUtils; //lowerLimit,upperLimit
import Quicksort;

class Lazysort extends Quicksort {
    public static void main(String[] a) {
        int[] L;
        int i;
		boolean d;
        Lazysort q;

        {
            i = 1 + 2 * 3;
            i = i * i;
        }
        L = new int[10];

        i = 1 + 2 + 3 + 4 * 5 + 6;

        i = 0;
        while (i < L.length) {
            L[i] = L.length - i;
            i = i + 1;
        }

        q = new Lazysort();

        q.quicksort(L);
        d = q.printL(L);
    }

    public boolean quicksort(int[] L) {
        boolean lazy;
        int rand;
        rand = MathUtils.random(0, 5);
        if (rand  < 4 ) {
            this.beLazy(L);
            L[1] = L[5];
            lazy = true;
        }
        else {
            lazy = false;
        }
        
        if ( lazy ) {
            lazy = !lazy;
        }
        else {
            lazy = this.quicksort(L, 0, L.length - 1);
        }

		rand = rand + rand + lazy + lazy * lazy;
        
        return lazy;
    }

}
